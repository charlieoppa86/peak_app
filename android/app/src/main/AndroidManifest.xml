<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 네트워크 (Supabase, Firebase, 날씨 API, AdMob) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 푸시 알림 권한 (Android 13 / API 33+, FCM 필수) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="Peakday"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- AdMob 앱 ID (AdMob 콘솔 > 앱 설정에서 확인, ca-app-pub-XXXXXXXX~XXXXXXXXXX 형식) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-REPLACE_WITH_YOUR_ADMOB_APP_ID"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 카카오 로그인 리다이렉트 핸들러 (카카오 계정 웹 로그인 → 앱 복귀) -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter android:label="flutter_web_auth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kakaoe941b08f8849d48f717f3088cd37710f" android:host="oauth" />
            </intent-filter>
        </activity>

    </application>

    <queries>
        <!-- 카카오톡 앱 로그인/공유 전환 시 패키지 가시성 필요 -->
        <package android:name="com.kakao.talk" />
        <!-- Flutter ProcessTextPlugin -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- share_plus: 텍스트 공유 -->
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- share_plus: 브라우저/앱 링크 열기 -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>

</manifest>
